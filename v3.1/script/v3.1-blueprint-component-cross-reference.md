# v3.1 Blueprint Element x Website Component Cross-Reference

**Audit date:** 28-08-2026
**Scope:** v3.1 Step 1, Step 2, Work Universe landing, Review Studio workspace, and the blueprint MVP phases in `goal/v3-blueprint.md` and `goal/v3-ui-blueprint.md`.  
**Rule:** a component is `BUILT` only when it is rendered and usable. Source code, a label, or a placeholder is not enough.

## Status key

| Status | Meaning |
|---|---|
| `BUILT` | Rendered, interactive, and backed by a real data or state contract. |
| `WIRED` | An existing built component is reached from the new Work Universe route. |
| `PARTIAL` | The useful core exists, but the blueprint asks for more. |
| `PARKED` | The current Step 3 spec explicitly says this is not shipped yet. |
| `WITHHELD` | The UI exists and correctly refuses to invent a value without evidence. |

## A. Entry, evidence, and workspace shell

| Blueprint element | Website component | Screen / route | Data or algorithm | Status | Check / remaining gap |
|---|---|---|---|---|---|
| Role / organisation / candidate ingress | `App.jsx` search modes | Step 1 | ESCO/ISCO role search, live job and company paths | `BUILT` | Existing Step 1 retained unchanged. |
| Live posting evidence selection | `McfJobsPanel` and posting picker in `App.jsx` | Step 2 | MCF/careers.gov.sg posting payload | `BUILT` | Evidence and withholding contracts are unchanged; card, index, overview and filter geometry now adapt to assessed phone/tablet size and orientation. |
| Responsive preflight template | `responsive/deviceProfile.js` + `App.jsx` root contract | Before Steps 2 and 3 | Synchronous stable-layout-viewport assessment using v2 mobile-first tiers plus RIN3 aspect/orientation rules | `BUILT` | Exposes form factor, size tier, orientation and aspect tier before either screen renders; iPhone/Samsung family is named only when the browser reports it, never guessed from dimensions. Software-keyboard visual-viewport changes do not reclassify the device. |
| Work Universe as Step 3 landing | `ReviewStudio.jsx` + `WorkUniverseLanding.jsx` | Step 3 first screen | Persistent three-panel shell with a guided `Start here` state | `BUILT` | The first read now leads with anchor, Work Units and evidence verification before the user deliberately opens the full five-graph map. The full-width Work Universe / Role Graph / Evidence Lineage banner is removed. |
| One workspace, two perspectives | Work Universe `Individual / Organisation` control with local `Target role / My evidence` focus | Step 3 centre and Evidence headers | `projectUniverse()` applies anchor-specific claim rules | `BUILT` | Role and person are no longer competing global perspectives. Person values remain withheld unless user-proven evidence exists. |
| Manual person-evidence ingress | `PersonEvidenceIngress.jsx` + `personEvidenceData.js` | Step 3 Person lens, top-left evidence rail | Session-only textarea plus manual target-skill confirmation | `BUILT` | Uses the accepted LeapView CV-overlay pattern. Raw text is never uploaded, stored or parsed. It becomes one unstructured proof only after confirmation; only manually ticked target skills become `USER-CONFIRMED` skills. Authority, access and allocation remain withheld. |
| Left Job Ad / Evidence Lineage rail | `WorkUniverseLanding.jsx` source pane + shared `review/job-ad-sections.js` contract | Step 3 left | Panel-owned `Job Ad / Lineage` switch; organisation-opportunity evidence when Organisation is active | `BUILT` | Job Ad retains Overview, Responsibilities, Work Units, posting-specific sections, Job Requirements and Skills A-Z. Lineage shows Source -> Derivation -> Claim. Organisation opportunities are supplied records only and explicitly do not establish headcount need. |
| Centre evidence canvas | Guided overview, five rounded DOM flow cards + Role Anchor, embedded Role Graph and embedded AI Moments | Step 3 centre | Authoritative DOM geometry from `ResizeObserver`; lazy-mount-then-hide Role Graph state | `BUILT` | Work Units are the first readable entry. The five graphs remain parallel projections, not process stages; Role Graph and AI Moments no longer require a separate Step 3 screen. |
| Right Contents / website tree | Work Universe outline pane | Step 3 right upper | Accessible expandable tree with current graph/detail selection | `BUILT` | Work Universe, anchor, five graphs, Role Graph, lineage, Governance Ledger and reviewer disagreement are usable; Organisation Work Graph expands to Organisation Map and AI Moments / Cards / Neural. The tree scrolls independently. |
| Right summary / detail inspector | Work Universe drill pane | Step 3 right lower | Graph, signal, evidence, interpretation, method, boundary | `BUILT` | Every first-order signal is a door into claim detail. |
| Contextual FAB navigation | `WorkUniverseLanding.jsx` + preserved `review/Canvas.jsx` navigator | Three-panel shell and evidence workspace | Desktop focus shortcuts; one mobile action sheet | `BUILT` | Work Universe FAB opens Contents, Job Ad evidence and - only in Organisation perspective - supplied organisation opportunities. Phones show one logical panel at a time with Workspace return. The preserved evidence-workspace FAB continues managing its windows and drawers. |
| Persistent Review Studio canvas | `ReviewStudioLegacy.jsx` + `review/Canvas.jsx` | Advanced evidence workspace | Lazy-mount-then-keep window state | `BUILT` | Company and Evidence remain available as drawers for deeper evidence review; the primary Role Graph now opens inside the centre panel. |
| Dock / float / pin / minimise | `Canvas.jsx` and `Desk.jsx` | Workspace | Viewport/zoom-corrected window geometry | `BUILT` | State is preserved per posting where specified. |
| Footer honesty contract | Work Universe footer + Review Studio footer | Both Step 3 surfaces | Source, confidence, time-window; AI-assisted/human decides | `PARTIAL` | Review Studio has the full triple; Work Universe shows level/state and standing line but not a time-window value. |

## B. Work Universe MVP and algorithms

| Blueprint / established MVP element | Website component | Algorithm / contract | Status | Check / remaining gap |
|---|---|---|---|---|
| Exactly five canonical graphs | `GRAPH_DEFS` in `WorkUniverseLanding.jsx` | Labour, Organisation Work, Intelligence, Human-Agent, Transition | `BUILT` | Browser gate requires all five visible. |
| Three signals inside every graph | Signal groups from `buildUniverse()` | Exactly 15 first-order claims, each with method and provenance | `BUILT` | Browser gate requires 3 per graph. |
| Centre-to-node proportions | `fit()` geometry calculation | Card widths, heights and anchor size clamped from measured frame width and height | `BUILT` | Wide screens use anchor/middle/right flow-card columns; phones use one vertical card stack with a separate branch spine. |
| Aspect-ratio / resize behaviour | `ResizeObserver` in `WorkUniverseLanding.jsx` | Recomputes `node`, `nodeSm`, `anchor` for the live centre pane | `BUILT` | No viewport-width-only assumption. |
| Typography scaling | Work Universe CSS custom properties and `clamp()` rules | Titles, flows, signal labels, methods, values scale from node diameter | `BUILT` | Small-width rules hide method text only when required for fit; method remains in detail. |
| Responsive desktop / tablet / mobile layout | Work Universe media rules | Desktop workspace is constrained to the available browser viewport with independent source/tree/detail scrolling; narrower screens stack the surfaces | `BUILT` | Chromium visual gate covers desktop/tablet/mobile, including the 2048 x 1280 desktop case. |
| Evidence-to-graph highlighting | `selectEvidence()` + `highlightedGraphs` | Evidence carries graph ids; linked nodes/connectors highlight and siblings dim | `BUILT` | Highlight is shape/stroke plus state, not colour alone. |
| Evidence-to-workspace round trip | `workspaceId` + structured `initialIntent` | Work Universe duty `D1` maps to Review Studio span `s0` | `WIRED` | Full browser assertion runs when a posting fixture supplies source rows; otherwise correct empty state is required. |
| Role reprojection | `projectUniverse(..., "role")` | Role-level duties, work units, skills, exposure and transition claims | `BUILT` | Uses supplied engine results; missing values are withheld. |
| Organisation reprojection | `projectUniverse(..., "org")` | Reframes claims as operating-model coverage, capability demand, information domains and operating allocation | `BUILT` | Does not claim organisation maturity from a job ad. |
| Person reprojection | `projectUniverse(..., "person")` | User-confirmed manual evidence plus normalised target-skill set difference | `BUILT` | Proof-only paste activates the person evidence anchor but does not create a skill claim. Without manually selected skills, personal skill delta stays withheld; access, authority and allocation are never read from raw text. |
| Restrained R3F enhancement | `WorkUniverseScene.jsx` | Low-opacity, pointer-free background layer | `BUILT` | Presentation geometry now echoes the rectangular card layout; DOM cards and buttons remain the geometry and accessibility authority. |

## C. Five graphs linked to website components

| Canonical graph | First-order website claims | Linked website component | Link behaviour | Status |
|---|---|---|---|---|
| Labour Graph | Source duties; distilled work units; canonical skills | Existing `RoleGraphPanel` | Opens the preserved Role Graph and FAB workspace | `WIRED` |
| Organisation Work Graph | OWG coverage; capabilities; authority | `OrganisationMap.jsx` + Company Information drawer + existing `CompanyPanel` AI Moments views | Opens the dedicated evidence-gated Organisation Map; Company Information remains available for employer facts; AI Moments routes to the preserved Cards / Neural displays | `BUILT` |
| Intelligence Graph | Intelligence objects; agent-actionable; human-heavy | AI Trace window + Evidence drawer | Opens both surfaces for trace inspection | `WIRED` |
| Human-Agent Graph | Functions; H/HY/A; exposure/authority gate | O-I-A cards + AI Trace windows | Opens both deterministic analysis windows | `WIRED` |
| Transition Graph | Changing first; formation; personal delta | Trajectory window | Opens the existing trajectory analysis | `WIRED` |

## D. Review manuscript and evidence system

| Blueprint element | Website component | Status | Check / remaining gap |
|---|---|---|---|
| Verbatim job-ad manuscript | `review/windows/Manuscript.jsx` | `BUILT` | Posting text is preferred; synthesis is labelled separately. |
| Source highlights and evidence spans | `buildDissection()` + Manuscript/O-I-A windows | `BUILT` | Every deterministic card joins on a real span id. |
| Persistent O-I-A inspector | `review/windows/Inspector.jsx` | `BUILT` | Present across the six analysis tabs and as the Evidence drawer. |
| Reviewer comments | `review/windows/Comments.jsx` | `PARTIAL` | Six shipped reviewer/lens voices; the complete blueprint persona set is parked. |
| Suggested rewrite + Accept / Reject | `rsComments()` and Comments window | `BUILT` | Decisions persist per posting. |
| Full track-change verbs | Review decision state | `PARKED` | Split, merge, relabel, escalate and first-class withhold actions are not built. |
| Deterministic source-to-comment connectors | `review/registry.jsx` + `Desk.jsx` | `BUILT` | Active trace, engine-known links, user-locked links, and edge stubs. |
| AI-suggested connector layer | `ReviewStudioLegacy.jsx` + `Desk.jsx` | `BUILT` | Off by default; dashed; accept promotes to a user link; dismiss removes it. |
| Critical Read lenses | `review/windows/Critical.jsx` | `BUILT` | Signal/noise, forensic reversal, falsification, hiring filter and advisory pass. |
| Requirements and hard gates | `GatesHard.jsx` + `QoI.jsx` | `BUILT` | Deterministic quality-of-information posture. |
| Role market evidence | Graphs, Salary, Indicators windows | `BUILT` | Role Graph, salary position and market indicators are reachable. |
| Additional v2+ analysis | Six `analysisPanes` in `App.jsx` | `BUILT` | AI Readiness, Understand Role, Market Position, Stewardship, Prepare, WikiGraph. |

## E. Visual grammar and organisation blueprint

| Blueprint element | Website component | Status | Check / remaining gap |
|---|---|---|---|
| Role evidence / occupation graph | `RoleGraphPanel` | `BUILT` | Layered, Knowledge and SSOC modes preserved. |
| AI exposure trace | `AITracePanel` / Aitrace window | `BUILT` | Deterministic duty rows and exposure evidence. |
| Obsidian-style linked knowledge view | Career `WikiGraph` analysis pane | `PARTIAL` | A linked graph exists, but it is not the full occupation-sensitive visual selector. |
| Organisation map | `work-universe/OrganisationMap.jsx` + `organisationMapData.js` | `BUILT` | A supplied-node organisation chart now leads the existing six dimensions: functions, reporting boundaries, dependencies, capabilities, authority and process ownership. The user-supplied org-chart reference grounds hierarchy/connector grammar; the professional chart reference grounds white-card, restrained-accent styling. Only supplied nodes, parent ids and relationship edges render. Agent-economy and Human-reserved-economy zones are explicit payload classifications: incomplete agent governance is blocked, incomplete human reservation is unconfirmed, and absence is withheld. No hierarchy, percentage or organisation maturity is inferred from a posting. The CTA continues to existing payload-driven AI Moments Cards / Neural views. |
| Workflow map | `work-universe/WorkflowMap.jsx` + `workflowMapData.js` | `BUILT` | Dedicated numbered process-flow surface answers who acts when. Only supplied `steps` / `stages` and explicit transitions, handoffs, dependencies, actors, decisions, queues and bottlenecks render; duties are never converted into sequence. Missing payload and unresolved links remain visibly withheld, and source ids route to the existing evidence path. |
| Value stream map | `work-universe/ValueStreamMap.jsx` + `valueStreamMapData.js` | `BUILT` | Dedicated organisation-perspective BPR surface follows the existing value-add / wait-handoff stage-card sample. It renders only supplied stages, classifications, timing/cost labels, owners, friction, customer impact, automation boundaries and AI-leverage proposals. Missing payload is withheld; no timing, waste, savings, layoffs, process quality or maturity is inferred, and ungoverned agent candidates are blocked. |
| InfraNodus-style concept map | No dedicated production component | `PARKED` | WikiGraph is not labelled or selected as an InfraNodus concept analysis. |
| Occupation-sensitive visual selector | `work-universe/OccupationVisualSelector.jsx` + `occupationVisualProfileData.js` | `BUILT` | The accepted Graph / Org / Workflow / Value Stream ribbon is usable and reversible. It recommends only when an explicit occupation visual profile supplies a supported primary visual and a source-linked reason; title-only, unlinked and missing profiles remain visibly withheld, while unsupported role-specific grammars are named as unavailable rather than silently remapped. |
| Hiring funnel / portfolio / site / control maps | No dedicated production components | `PARKED` | Named by the blueprint, intentionally unshipped in the current Step 3 spec. |

## F. Candidate outputs and governance

| Blueprint element | Website component | Status | Check / remaining gap |
|---|---|---|---|
| Candidate task preparation | Prepare analysis window | `PARTIAL` | Task prep and foundation guidance exist; proof tasks are not a unified output ledger. |
| Interview preparation | Critical Read / Prepare material + `review/PrintPackage.jsx` | `BUILT` | The package consolidates traceable questions into one interview sheet and withholds it when posting evidence is insufficient. |
| Resume alignment | `PersonEvidenceIngress.jsx` + resume engine code + evidence-gated rationale in `review/PrintPackage.jsx` | `PARTIAL` | Direct session-only manual paste and user-confirmed target skills now reach the printable set-difference rationale. Structured work-history/proof editing and the source-linked per-claim rewrite workbench remain unbuilt. |
| Cover-letter workbench | Earlier component/code exists but is not linked from current Step 3 | `PARKED` | Must cite accepted evidence before being restored. |
| Structured export | OKF + provenance-separated JSON export | `BUILT` | Deterministic and AI-authored blocks are separated. |
| Print / PDF / all-markup package | `review/PrintPackage.jsx` | `BUILT` | Clean and Full Review packages print as A4 editorial pages with reviewer summary, decision ledger, candidate actions, interview sheet and evidence-gated resume rationale. |
| Governance ledger | `work-universe/GovernanceLedger.jsx` + `governanceLedgerData.js` + `review/PrintPackage.jsx` | `BUILT` | A dedicated, printable ledger renders only supplied decisions, evidence, deterministic results, AI interpretations, risk, scope, named human owner, allowed/forbidden actions, guardrails, transparency, audit and override path. A record is `ALLOWED` only when the required control contract is complete; a supplied status cannot bypass missing controls. AI interpretation additionally requires a complete supplied agent-identity disclosure. Human review supports open, accepted, rejected, resolved and escalated states; session actions never invent actor identity or timestamps. |
| Multi-agent disagreement display | Disagreement tab in `work-universe/GovernanceLedger.jsx` + printable audit section | `BUILT` | Only explicitly supplied competing reviewer positions render. Reviewable conflict requires at least two complete reviewer/claim/evidence positions plus a supplied human decision question; incomplete or absent conflict remains withheld. The human can open, accept, reject, resolve or escalate, and the state reaches the Full Review print package. |

## Honest completeness conclusion

The established Role Work Universe MVP is now built and linked to the existing v3.1 evidence workspace. The full v3 blueprint is **not** complete. The largest remaining product blocks are other role-specific maps, the traceable cover-letter workbench, the structured candidate-proof workflow, and the remaining full track-change verbs.

The next build should start with the **structured candidate-proof workflow**, because person evidence now has a safe session ingress and printable set-difference rationale, but accepted work-history claims still lack a unified source-linked proof ledger and rewrite surface.
